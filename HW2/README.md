# Homework 2

_Выполнил Бергман Валерий_

Код в формате ноутбука, запуск стандартный в среде выполнения с GPU, (num_workers = 32, так как на сервере столько ядер).

Обучил модель DETR на подмножестве набора COCO. Выбрал по нижней границе десять классов (селекчу их из COCO в generate_synthetic_dataset и нижележащих функциях).

```py
'target_categories': ['person', 'car', 'dog', 'chair', 'pizza', 'bus', 'bottle', 'train', 'horse', 'bird'],
```

Трейн препроцессил, делал аугментации: флипы, кропы, размытия. Использовал библиотеку Albumentations для валидации.


mAP'ы получил 0.5 ≈ 0.40, 0.75 ≈ 0.31, 100 ≈ 0.53.

Анализ показывает, что при изменении параметра с 0.5 до 0.75 качество модели падает. Тут можно сказать, видимо, что моделька часто промахивается рамками и ставит их неточно, захватывая объект не идеально.
